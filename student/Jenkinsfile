pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
              echo "Build"
            }
        }
        stage("docker build") {
            steps {
                echo "docker build"
            }
        }
    }
}
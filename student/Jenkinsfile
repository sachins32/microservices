pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
              mvn clean package -DskipTests
            }
        }
        stage("docker build") {
            steps {
                echo "docker build"
            }
        }
        stage("deploy") {
                    steps {
                        echo "deploy to k8s"
                    }
                }
    }
}